@{

}

<script>

    function getpersondata(tableID) {
        addRowSpelers(tableID, document.getElementById("voornaam").value, document.getElementById("achternaam").value);  
    }

    function addRowSpelers(tableID, voornaam, achternaam) {
        let tableRef = document.getElementById(tableID);

        let newRow = tableRef.insertRow(-1);

        let newCell2 = newRow.insertCell(0)
        newCell2.appendChild(document.createTextNode(voornaam));

        let newCell3 = newRow.insertCell(1)
        newCell3.appendChild(document.createTextNode(achternaam));

        let newCell4 = newRow.insertCell(2)
        var t4 = document.createElement("input");
        t4.type = "button";
        t4.value = "Delete";
        t4.onclick = function () { SomeDeleteRowFunction(this); };
        newCell4.appendChild(t4);
    }

    function SomeDeleteRowFunction(o) {
        var p = o.parentNode.parentNode;
        p.parentNode.removeChild(p);
    }
</script>

<div class="row">
    <div class="col-4">
        <div class="input-group mb-3">
            <div class="input-group-prepend">
                <h2>Spelers</h2>
            </div>

            <div class="input-group-prepend">
                <input type="text" class="form-control" id="voornaam" placeholder="Voornaam" aria-label="Sizing example input" aria-describedby="inputGroup-sizing-default">
            </div>

            <div class="input-group-prepend">
                <input type="text" class="form-control" id="achternaam" placeholder="Achternaam" aria-label="Sizing example input" aria-describedby="inputGroup-sizing-default">
            </div>

            <div class="input-group-prepend">
                <button class="btn btn-outline-secondary" onclick="getpersondata('spelerstable');" style="width: 100%;" type="button" aria-label="Sizing example input" aria-describedby="inputGroup-sizing-default">Persoon toevoegen</button>
            </div>
        </div>
    </div>

    <div class="col-8">
        <table class="table table-bordered" id="mytable">
            <thead>
                <tr>
                    <th>Voornaam</th>
                    <th>Achternaam</th>
                    <th></th>
                </tr>
            </thead>
            <tbody id="spelerstable">

            </tbody>
        </table>
    </div>
</div>